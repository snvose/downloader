python-telegram-bot>=21.0
yt-dlp>=2024.1.1
gallery-dl>=1.26.0
python-dotenv>=1.0.0
requests>=2.31.0
mutagen>=1.47.0

# TLS fingerprint impersonation (yt-dlp impersonate). yt-dlp uses this on
# sites that detect and block bot clients; without it, those sites fail
# silently.
# UPPER BOUND REQUIRED: yt-dlp only accepts versions 0.5.10 and 0.10.x-0.15.x
# (yt_dlp/networking/_curlcffi.py). Installing 0.16 silently disables the
# package — the impersonate target list comes back empty with no error
# shown anywhere.
curl_cffi>=0.10,<0.16
